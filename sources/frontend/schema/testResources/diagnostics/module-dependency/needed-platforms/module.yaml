product:
  type: kmp/lib
  platforms:
    - jvm
    - android
    - js

dependencies:
  - <![Error] Module `sub-module` does not support compiling to platforms [`jvm`, `android`]!>./sub-module<!>

dependencies@jvm:
  - <![Error] Module `sub-module` does not support compiling to platforms [`jvm`]!>./sub-module<!>

dependencies@android:
  - ./jvm-sub-module
  - ./jvm-wasm-sub-module

dependencies@js:
  - ./sub-module
