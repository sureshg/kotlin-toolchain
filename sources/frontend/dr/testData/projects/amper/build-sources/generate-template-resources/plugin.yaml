
tasks:
  generateTemplateResources:
    action: !generateTemplateResources
      outputDir: ${taskOutputDir}
      templatesDir: ${module.rootDir}/resources/templates

generated:
  resources:
    - directory: ${tasks.generateTemplateResources.action.outputDir}