product: jvm/lib

settings:
  publishing:
    mavenCentral: <![Warning] Maven Central publishing is enabled, but publishing itself is disabled!><![Error] Maven Central requires artifact signatures, but signing is disabled. Enable it using `settings.publishing.signArtifacts`.!><![Error] Maven Central requires publishing sources, enable it using `settings.publishing.publishSources`.!><![Error] The following properties are unset, but required for Maven Central publication:  - `description`  - `publishing.group`  - `publishing.pom.developers`  - `publishing.pom.licenses`  - `publishing.pom.scm.url`  - `publishing.pom.url`  - `publishing.version`!>enabled<!><!><!><!>