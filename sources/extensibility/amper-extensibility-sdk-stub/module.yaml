product: jvm/lib

apply:
  - //sources/common.module-template.yaml

description: |
  A module with the minimal subset of `JDK` and `kotlin-stdlib` entities that we need in Kotlin schema analysis

settings:
  publishing:
    enabled: false

  kotlin:
    freeCompilerArgs:
      - -Xallow-kotlin-package